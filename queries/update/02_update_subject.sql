-- UPDATE TAB2 SET (поле1, поле2) = (значение1, выражение(поле2))
-- Изменение названия и часов дисциплины с кодом 1

\echo '=== ДО UPDATE ==='
SELECT код, название, часы FROM ДИСЦИПЛИНЫ WHERE код = 1;

UPDATE ДИСЦИПЛИНЫ 
SET (название, часы) = ('Алгоритмизация и программирование', часы + 36) 
WHERE код = 1;

\echo '=== ПОСЛЕ UPDATE ==='
SELECT код, название, часы FROM ДИСЦИПЛИНЫ WHERE код = 1;
