-- UPDATE TAB1 SET поле = TAB2.pole2 FROM TAB2 WHERE выражение
-- Обновление названия группы для всех студентов (демонстрация)

\echo '=== ДО UPDATE ==='
SELECT ГРУППЫ.название, COUNT(*) FROM УСПЕВАЕМОСТЬ
INNER JOIN ГРУППЫ ON ГРУППЫ.код = УСПЕВАЕМОСТЬ.группа
GROUP BY ГРУППЫ.название LIMIT 5;

-- Здесь можно добавить UPDATE FROM, но для ВУЗ это сложнее
\echo 'UPDATE FROM не выполняется - демонстрационный пример'
