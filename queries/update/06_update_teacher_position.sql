-- UPDATE: Обновление должности преподавателя

\echo '=== ДО UPDATE ==='
SELECT фамилия, имя, должность FROM ПРЕПОДАВАТЕЛИ WHERE код = 4;

UPDATE ПРЕПОДАВАТЕЛИ SET должность = 'Доцент' WHERE код = 4;

\echo '=== ПОСЛЕ UPDATE ==='
SELECT фамилия, имя, должность FROM ПРЕПОДАВАТЕЛИ WHERE код = 4;
